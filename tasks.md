# Project Task Sequence

## Starting with Arrays Data structure
- [x] Design the layout for the visualization 
- [X] Design the Algo for the basic Bubble Sort Algo.
- [X] Design the `Psudo Code View Interactive tool` which will follow the algorithm
- [X] Design More Components to the swap logic like arrows pointer `(i,j,k)`
- [X] Add Insertion Sort 
- [X] Add Bubble Sort (Partially done pointer adding is pending)
- [X] Add Selection Sort (Partially done pointer adding is pending) 
- Add Quick Sort 
- Add Merged Sort
- Add the timeline that will show the current animation which can be interactive like play in `2x or 3x` for speed up the animation
- Create an Constants variable that will tell about all the dimenstion.(fields values like width,height,borders,etc).
- Fix the bug that happen after re-clicking the sort btn once the array are sorted (pre-check the array weather it is sorted our not)
- Final Task for this section `Create an document Page Blog page` that will guide the algoritms details 



## Utility Tasks
- Document the code